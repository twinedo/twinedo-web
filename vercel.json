{
  "buildCommand": "npm run build",
  "env": {
    "SKIP_ENV_VALIDATION": "true"
  },
  "framework": "nextjs",
  "functions": {
    "src/app/api/[[...slugs]]/route.ts": {
      "maxDuration": 30
    }
  },
  "installCommand": "npm install && cd src/backend && npm install && npx prisma generate && cd ../.."
}